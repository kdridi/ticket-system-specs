---
id: TS-012
title: "Add /ticket-system-doctor command for consistency verification and recovery"
status: rejected
priority: P0
type: feature
created: 2026-04-04 12:00:00
updated: 2026-04-05 02:50:01
dependencies: []
assignee: unassigned
estimated_complexity: medium
---

# TS-012: Add /ticket-system-doctor command for consistency verification and recovery

## Objective
Add a diagnostic command that verifies the consistency of the ticket system state and proposes corrections when inconsistencies are detected. This covers crash recovery, orphaned worktrees, status/directory mismatches, and stale roadmap entries.

## Context
The ticket system performs multi-step operations (git mv + frontmatter update + roadmap edit + commit). If any step fails mid-operation (Claude crash, network interruption, disk full), the system is left in an inconsistent state with no recovery mechanism. The user must manually diagnose and fix the problem, which requires deep understanding of the system internals.

Identified as the highest-priority missing feature across two independent audit reviews.

## Acceptance Criteria
- [ ] A new skill `/ticket-system-doctor` exists with its own SKILL.md
- [ ] The command uses the `ticket-system-reader` agent (read-only diagnostics)
- [ ] The command checks: frontmatter `status` matches the directory the ticket is in (backlog/planned/ongoing/completed/rejected)
- [ ] The command checks: orphaned git worktrees (worktree exists but no corresponding ticket in ongoing/)
- [ ] The command checks: stale roadmap entries (ticket referenced in roadmap but not in planned/)
- [ ] The command checks: multiple tickets in ongoing/ (should be max 1)
- [ ] The command checks: incomplete transactions via `.tickets/.pending` file
- [ ] Each issue is reported with a clear description and a suggested fix command
- [ ] The command does NOT auto-fix — it reports and suggests. User decides.
- [ ] `disable-model-invocation: false` (safe to auto-invoke, read-only)

## Technical Approach
Add to specs.md:
- New skill `ticket-system-doctor/SKILL.md` with `context: fork`, `agent: ticket-system-reader`
- Diagnostics checklist executed in order:
  1. Read `.tickets/config.yml`
  2. Check for `.tickets/.pending` (incomplete transaction from a previous crash)
  3. Scan all ticket files, verify `status` field matches parent directory
  4. Run `git worktree list`, cross-reference with tickets in ongoing/
  5. Read roadmap, verify each referenced ticket exists in planned/
  6. Check that ongoing/ contains at most 1 ticket
  7. Report findings as a structured checklist with [OK] / [ISSUE] prefixes

For the `.pending` transaction log, add an instruction to all mutative commands (schedule, plan, merge, abort): write a `.tickets/.pending` file before starting multi-step operations, delete it on successful completion. Format:
```yaml
operation: plan
ticket: PREFIX-XXX
started: 2026-04-04 12:00:00
description: "Activating ticket — creating worktree and moving to ongoing"
```

## Dependencies
<!-- None -->

## Files Modified
- `specs.md` (sections 2.3, 2.4, 4.2, 5.1)

## Decisions
<!-- To be filled during implementation. -->

## Notes
- The reader agent already has the necessary bash permissions (cat, find, grep, git worktree list via extension if needed).
- The .pending mechanism is lightweight — one file write at the start, one delete at the end.
- Future enhancement: a `--fix` flag that auto-applies suggested corrections (would require a different agent with write permissions).

## Sub-tickets
- TS-027: "Add /ticket-system-doctor diagnostic skill (read-only checks)" — planned, position 2
- TS-028: "Add .pending transaction log to all mutative commands" — planned, position 3, depends on TS-027

## Log
- 2026-04-04 12:00:00: Ticket created from audit review (W2 + C1).
- 2026-04-05 02:50:01: Split into TS-027 and TS-028 (atomicity: cross-cutting layers High). Rejected — objective delivered via sub-tickets.
